# Local fonts

Place font files referenced by `next/font/local` here.

Currently expected:

- **BebasNeue-Regular.ttf** — used as a test on the home page only
  (loaded in `app/page.tsx`). Drop the file in this folder exactly with
  that name, or the home page build will fail with a "module not found"
  error for the font.
